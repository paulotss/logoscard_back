{
  "compilerOptions": {
    /* Language and Environment */
    "target": "es2020", // Sugestão: es2020 é um alvo mais moderno e compatível
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,

    /* Modules */
    "module": "commonjs",
    "rootDir": "./src", // Aponta para a raiz do seu código-fonte
    
    // ADIÇÃO CRUCIAL 1: Informa ao TS onde encontrar as definições de tipo
    "typeRoots": [
      "./node_modules/@types",
      "./src/@types"
    ],
    
    /* Emit */
    "outDir": "./dist",

    /* Interop Constraints */
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true,

    /* Type Checking */
    "strict": true,

    /* Completeness */
    "skipLibCheck": true
  },
  // ADIÇÃO CRUCIAL 2: Informa ao TS quais arquivos compilar
  "include": [
    "src/**/*" // Garante que o TS compile tudo dentro da pasta 'src'
  ],
  "exclude": [
    "node_modules"
  ]
}